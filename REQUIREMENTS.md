## Dependencies
```
pip install PyQt6 chromadb google-generativeai requests python-dotenv
```

### Optional (for local Ollama models)
- Install [Ollama](https://ollama.com/) and pull a model:
  ```
  ollama pull gemma:2b
  ```
